# 📒 Handoff Log — สารบัญ

บันทึกส่งต่องานของโปรเจกต์ Flash Sale System (mini-project backend mobile)
**เรียงใหม่สุดอยู่บนสุด** · รูปแบบชื่อไฟล์: `handoff_<DD_MM_YYYY>_<หัวข้อ>.md` — **วันที่ขึ้นก่อนหัวข้อ** เพื่อให้ `ls` เรียงตามเวลาให้เอง

> กติกา: ห้ามทับไฟล์เดิม (ชื่อชนให้เติม `_2`, `_3`) · ใช้วันที่จริงเสมอ ห้ามเขียน "เมื่อวาน / สัปดาห์ที่แล้ว"

---

- 2026-08-31 — [sync เอกสารตาม config ที่รวมงาน 2 คน + ของที่หายตอน merge](handoff_31_08_2026_doc-sync-and-merge-findings.md) — วันเดียวกันมีคนแก้ config **2 คน** แล้ว merge กัน เอกสารตามไม่ทันทั้งสองฝั่ง · sync `.md` **8 ไฟล์**: `WORKER_CONCURRENCY` **5 → 1** (รวมคลัสเตอร์ 30 → **6** · ยืนยันด้วย `docker exec` ว่า env ชนะ `|| 5` ใน `orders.processor.ts:40`), app `cpus` **0.75 → 1.0**, `keepalive` **128 → 768** · 🔴 **เจอของหายตอน merge**: `LOG_LEVEL: warn` ทั้ง 6 service หายใน `e16476d` (มีใน `4b77a22` → หลัง merge เหลือ 0) · 🔴 **และวิเคราะห์แล้วว่ามันไม่ได้ทำในสิ่งที่ comment บอก จึงจงใจไม่ใส่กลับ** — read ถูก sample 1/100 อยู่แล้ว (`logging.interceptor.ts:65-66`) ตั้ง `warn` จึงประหยัดแค่ ~50 บรรทัด/วิ แต่**ปิด log `202` ของ write path 100%** ซึ่ง §5 ข้อ 5 ต้องการ · ส่วนตัวที่กิน CPU จริงคือ error branch (`logger.warn` ไม่ sample — **226,618 × 409** ในรอบที่วัด) ซึ่ง `warn` **ไม่แตะเลย** · ตัวที่ควรแก้จริงคือ sample error branch เฉพาะ 409/429 → ติด §8 + ต้องคุยกับคนเขียน `4b77a22` ก่อน · **หลักการ sync**: ตรงไหนที่เอกสารอธิบาย *ทำไม* ถึงเลือกค่านั้น → เก็บเหตุผลเดิม เพิ่มหมายเหตุ ไม่ทับทิ้ง (ADR-6 "ทำไม 5 ไม่ใช่ 50" ยังถูก · เหตุผลที่ลดเหลือ 1 เป็นคนละเรื่อง) · **ไม่แตะเอกสารประวัติศาสตร์** (`LOADTEST_AND_SCALING_REPORT.md`, handoff เก่า — `keepalive 128` ถูก ณ วันนั้น) และ `02-design-review-qa.md` (เลข 5/30 เป็นส่วนของบทถกเถียง ADR ไม่ใช่สถานะปัจจุบัน) · ✅ build/lint/test **49 tests** · 11/11 healthy · `down && up` 18.7 วิ · contract §3 ตรวจมือครบ · load balance 6/6 · **§9.3 ผ่านครบ 4 ข้อทั้งที่รอบนั้นเจอ 5xx 2,730 ครั้ง** (compensation ทำงานถูกภายใต้ความกดดันจริง) · ❌ **ยังไม่มีใครยิง k6 บน config ชุดรวมนี้เลย** และยังไม่เคย deploy ขึ้น VM 4 core — ตัวเลขทุกตัวยังมาจาก dev Mac ที่ noise 7.7 เท่า
- 2026-08-31 — [ผูก published port ของ datastore กับ `127.0.0.1` (ปิดรู §0.1 ก่อนขึ้น VM)](handoff_31_08_2026_bind-datastore-loopback.md) — ปิดรูที่ §0.1 มาร์ก 🟠 ไว้ตั้งแต่ 08-30 **"datastore เปิดทุก interface ไม่มีรหัสผ่าน"** ด้วยการเติม prefix `127.0.0.1:` หน้า port mapping ของ postgres ×2 + redis ×2 · **`nginx :8080` จงใจไม่แตะ** เพราะกลุ่มอื่นต้องยิง k6 เข้ามาได้ · 📌 **แก้คำพูดใน §0.1 ที่เคยเขียนผิด**: `SET stock:... 9999` จาก Redis **ไม่ได้** "ข้ามทั้ง 4 ชั้น" — Tier 3 (`WHERE remaining_stock > 0`) กับ Tier 4 (`CHECK >= 0`) ยังกัน oversell อยู่ อาการจริงคือ 202 แล้ว job ตายเป็น `SoldOutError` · **ตัวที่ข้ามได้ทุกด่านจริงคือ Postgres** (รหัส `flashsale`/`flashsale` อยู่ในไฟล์ที่ push ขึ้น GitHub public) เพราะ Tier 3/4 อยู่ *ใน* Postgres เอง · ตรวจแล้ว **ไม่กระทบอะไรเลย** — ทุกตัวที่ต่อ datastore ใช้ loopback อยู่แล้ว (`reset.ts:54-55`, `data-source.ts:52-53`, `cache-stats.sh:12`, `loadtest/*.sh|ps1`) และ container คุยกันผ่าน Docker network ไม่ผ่าน published port · ยืนยันด้วยการยิงจริง: จาก LAN IP → PG/Redis **BLOCKED ครบ 4**, nginx 8080 ยัง OPEN · `pnpm run reset` + `psql -h localhost` ยังทำงาน · build/lint/test **49 tests** ผ่าน · 11/11 healthy · ⏹️ **จงใจไม่ใส่ `requirepass`** — จะลามไปแก้ `redis.module.ts` + BullMQ + env ทั้งหมด ทั้งที่ loopback ปิดช่องเดียวกันได้ด้วย 4 บรรทัด · 🟠 **รูที่ยังเหลือ: Bull-Board `admin`/`admin` hardcode** ซึ่งเปิดผ่าน `:8080` ที่ยังต้องเปิดสู่ LAN
- 2026-08-31 — [Right-size `docker-compose.yml` ให้ตรงกับ VM เป้าหมาย 4-core/6GB (ลบ app-7/app-8)](handoff_31_08_2026_right-size-to-4core-vm.md) — พบว่า compose ดริฟต์ไปใช้ **8 instance + `cpus 1.5`** โดยไม่มี commit/handoff บันทึกไว้เลย ทั้งที่ `architecture.md` §8.1/§8.2 เขียนไว้ตั้งแต่ 08-28 แล้วว่า VM เป้าหมายมีแค่ 4 core และ app tier ควรเป็น 6 instance × `cpus 0.75` — แก้กลับให้ compose ตรงกับเอกสาร (**cpus รวม 18.0 → 8.5 · mem_limit รวม 7,424 → 5,440 MB** เทียบกับ VM 4-core/6,144MB: เดิมเกิน RAM จริง 121%, ใหม่ 88.5%) · `redis-data` จงใจไม่แตะ (§8.2 ข้อ 2 = คอขวดอันดับ 2) · เพิ่ม `enableAutoPipelining` ใน `redis.module.ts` + ถอด `ValidationPipe` ซ้ำใน `products.controller.ts` · build/lint/test ผ่าน **49 tests**, §9.3 ผ่านครบ 4 ข้อ · ⚠️ **วัด performance ทั้งหมดบน dev Mac 8-core ที่รัน k6 พร้อมกันด้วย ไม่ใช่ VM เป้าหมาย** — noise สูงมาก (7 รอบ 200 VUs เดียวกันได้ 700–5,406 rps ต่างกัน 7.7 เท่า) จึง **ยังพิสูจน์ 6-vs-8 instance ไม่ได้เลย** และหลังแก้ config ยิงซ้ำบนเครื่องเดิมกลับได้ผล**แย่กว่าเดิม** (5xx 1,308 ครั้ง จาก 0) — สมมติฐานคือ cgroup CPU ceiling ไปบีบคอเครื่อง dev 8-core ซึ่งอาจไม่เกิดบน VM 4-core จริง **ยังไม่ได้ยืนยัน ต้องยิงบน VM จริงก่อน**
- 2026-08-30 — [รีวิว 6 agent (scrutinize + karpathy) → เก็บกวาดเฉพาะส่วนที่ปลอดภัย](handoff_30_08_2026_six-agent-review-and-safe-cleanup.md) — `git pull` ตามหลัง 9 commits แต่ **ไฟล์บนดิสก์ตรงกับ remote ทุก byte อยู่แล้ว** (SynologyDrive มาก่อน git) · รีวิว 6 agent เจอ **บั๊ก 2 · เอกสารเคลมเท็จ 2 · ช่องโหว่ทดสอบ 2 · โค้ดตาย ~205 บรรทัด** · 🔴 **`orders.processor.ts:63-64` — `connect()`/`startTransaction()` อยู่นอก `try` → เป็น path เดียวที่หักสต็อกแล้วไม่มีทางชดเชยเลย** (ยังไม่แก้ ต้องยิง k6 ก่อน) · 🏆 ข้อค้นพบดีสุด: **debounce ซ้อน 3 ชั้นเป็นตัว *สร้าง* บั๊ก** — ใช้กลไกเดียวไม่มีทางทิ้งงาน เหลือ trailing timer อย่างเดียว −40 บรรทัดและบั๊กเกิดไม่ได้ · 🟠 **datastore เปิดทุก interface ไม่มีรหัสผ่าน + Bull-Board `admin`/`admin` hardcode** (แก้ `.env` ไม่มีผล) · ✅ แก้จริงเฉพาะ "กลุ่มสบายใจ": **บั๊ก DOM leak 1,200 div/ชม. ในหน้า `/admin/insights`** + ลบโค้ดตาย 41 บรรทัด + คอมเมนต์โกหก 4 จุด · **ไม่ต้องยิง k6 เพราะพิสูจน์ด้วย `dist/` byte-identical (SHA-1)** · ⚠️ พบว่า `insights.page.ts` เป็น template string ที่ **build/lint/test มองไม่เห็นข้างในเลย** ต้อง gate ด้วย `new Function()` · build/test ผ่าน **49 tests**
- 2026-08-30 — [Sync เอกสาร Architecture กับ observability + แก้ compensation guard](handoff_30_08_2026_arch-doc-sync-and-compensation-guard-fix.md) — เอกสารตามโค้ดไม่ทัน **+1,729 บรรทัด / 20 ไฟล์** (observability layer ที่เพื่อน push มา) แก้ด้วย 3 agent แบ่งไฟล์ · ของแถม: **`file:line` เดิมเพี้ยน 17 จาก 24 จุด** (หนักสุด `redis.service.ts` ~56 บรรทัด) + แก้คำตอบ Q&A ที่เป็นเท็จ 2 ข้อ · **แก้บั๊ก 2 ตัว**: guard key `compensated:{jobId}` → `{jobId}:{requestToken}` (เดิม idempotent ข้าม *คำขอ* ไม่ใช่ retry → หายถาวร 1 ชิ้น) และ read path เงียบตอน stock key หาย (เพิ่ม metric แยก + log throttle 10 วิ เพราะ 1,000 VUs จะล้าง Docker log ring 30MB ทิ้ง) · **`IntegrityService` ไม่ได้รันทุก 3 วิ** — ไม่มี timer ฝั่ง server เลย 3 วิคือ `setInterval` ในเบราว์เซอร์ → ปิดแท็บ = ไม่มีใครตรวจ · ⏹️ **ตรวจแล้วตัดสินใจไม่แก้**: `compensate()` ไม่มีเงื่อนไขตอน `queue.add` ล้ม (Redis สูงกว่า DB ซ่อมเอง / ต่ำกว่าไม่ซ่อม · `compensate-if-reserved.lua` เป็น no-op ตรงนั้น) — บันทึกลง §0.1 กันคนมาแก้ซ้ำ · build/lint/test ผ่าน **49 tests** · ⚠️ **ยังไม่ได้ยิง k6 หลังแก้**
- 2026-08-30 — [Karpathy review 6 agent + เช็คลิสต์ก่อนส่ง](handoff_30_08_2026_karpathy-review-and-submission-todo.md) — **📌 อ่านไฟล์นี้ก่อนถ้าจะทำงานส่ง** · อ่านโจทย์ต้นฉบับได้ภาษาไทยครบครั้งแรก (ต้องใช้ `pdftotext -enc UTF-8` ไม่งั้น glyph ไทยหายหมด) เจอของใหม่ 2 อย่าง: โจทย์บังคับ **"แคปเจอร์หน้าจอ Database"** ไม่ใช่ paste SQL · และโจทย์คาดว่า "ของหมด/ซื้อซ้ำ" จะโผล่ที่ **Failed Jobs** แต่ดีไซน์เราปฏิเสธก่อนเข้าคิว → **Bull-Board จะโชว์ Failed = 0** ต้องเขียนอธิบายกันเข้าใจผิด · รีวิวโค้ด 6 agent ผ่าน skill karpathy — เฉลี่ย **6.7/10** (แกนกลางแข็ง 7-8 แต่ Goal-Driven ร่วงทุกส่วนที่ออกจาก core) · **finding ที่ยังไม่แก้ 10 ข้อ** ที่แรงสุด: `bullmq.module.ts` ไม่มี `commandTimeout` ทำให้ `catch` ตาย · `compensate()` แบบไม่มีเงื่อนไขขัดกับเหตุผลในไฟล์ตัวเอง · `/health/ready` ไม่มีใครใช้จริง · `request.user.sub` เป็น branch ตาย + token ไม่มี subject ผ่านได้ · เอกสารล้วน ไม่แตะ `src/`
- 2026-08-30 — [sync `Report_flash-sale-report.md` ให้ตรงกับโค้ดจริง + เช็ค Synology Drive/Git](handoff_30_08_2026_report-sync-with-codebase.md) — ไล่รายงานทีละข้อความเทียบ `src/` เจอไม่ตรง **6 จุด** แก้หมดแล้ว (ที่ใหญ่ที่สุด: DFD เขียน `DEL catalog:page:*` ขัดกับกฎห้ามใช้ `KEYS` ของรายงานเอง · §2.4 ตกกับดัก **BullMQ dedup ที่คืน job เดิมเงียบ ๆ ไม่ throw** · ตารางชดเชยขาด 2 กรณี) · **Synology Drive ยังไม่มีอาการพัง** (`.git` ปกติ ไม่มี `index.lock` ไม่มี conflicted copy) แนะนำตั้ง File Filter ยกเว้น `.git`/`node_modules`/`dist` — **ยังไม่ยืนยันว่าตั้งแล้ว** · ⚠️ ตอน push เจอเพื่อน push **observability layer** มาก่อน (30 ไฟล์) ต้อง merge แล้ว sync รายงานรอบสอง (tests 35 → **43** · เพิ่ม §3.8 `/admin/insights` · "ไม่มี reconciliation" กลายเป็นเท็จ) — **ของเพื่อนเป็น instrumentation ล้วน control flow ไม่ถูกแตะ** · เอกสารล้วน ไม่แตะ `src/`
- 2026-08-29 — [พิสูจน์ผล audit กับระบบจริง + ถอด dead-code pipe + สร้าง Google Doc รายงาน](handoff_29_08_2026_verify-audit-and-report-doc.md) — **ยิงจริงหักล้างข้อสรุป audit ข้างล่าง**: ที่ว่า orders ตอบ 400 แล้ว "กระทบยิงข้ามกลุ่ม" **ไม่จริง** (`{productId, quantity}` ได้ 202) เพราะ global pipe รันก่อน param pipe เสมอ `forbidNonWhitelisted` จึงเป็น **dead code** · ถอดออก (comment โกหก + เป็นกับดักถ้ามีคนแก้ main.ts) · บันทึก 400/503 ที่มีอยู่จริงลง CLAUDE.md §3 · ปิดคำถาม `23514` ว่า **unreachable by design** · แก้ error mapping ที่ผิดใน architecture.md §6 · **📄 Google Doc รายงานเสร็จแล้ว** (เหลือเติมรายชื่อสมาชิก/ตารางเทียบกลุ่มเพื่อน/screenshot) — build/lint/test 35 ข้อผ่านครบ
- 2026-08-29 — [ตรวจเอกสารตรงกับโค้ดจริงไหม (3 agent) + แก้ diagrams.md/architecture.md](handoff_29_08_2026_doc-accuracy-audit.md) — แก้ doc-only staleness หมดแล้ว (compensate path ที่หายไปจาก DFD/CFD, code sample เก่า, bottleneck ที่แก้แล้ว 2 ข้อ) — **⚠️ ข้อสรุปของบันทึกนี้ถูกพิสูจน์ว่าผิดบางส่วนแล้ว อ่านกล่องแดงบนสุดของไฟล์ก่อน** · ทั้ง 2 คำถามที่ค้างปิดหมดแล้ว ไม่ต้องถามผู้ใช้อีก
- 2026-08-29 — [git pull + ยิง k6 ซ้ำบน local (resource contention ไม่ใช่บั๊กใหม่)](handoff_29_08_2026_git-pull-k6-retry-local.md) — pull + merge สำเร็จ, เจอ nginx.conf lock กับ SynologyDrive (แก้ด้วย local-only `docker-compose.override.yml`), ts-node ช้าเพราะอ่านไฟล์ผ่าน synced fs — **k6: §9.3 data integrity ผ่านครบ (50/50, remaining_stock=0) แต่ latency/504 แย่กว่ารอบ 08-28 มาก สงสัยว่า CPU ของเครื่องนี้ไม่พอ (8 core เท่ากับที่ Docker VM จองไปหมด) ยังไม่ยืนยัน 100%** — **⚠️ งานถูกตัดจบกลางคันตามคำขอผู้ใช้**
- 2026-08-29 — [Production VM ต้อง login PSU passport ก่อนออกเน็ต](handoff_29_08_2026_vm-psu-passport-login.md) — `172.30.58.5` ping ได้แต่ port 80 refused (ไม่ใช่ web server) ต้อง SSH เข้าไป login captive portal จากฝั่ง VM เอง ไม่ใช่เปิดเบราว์เซอร์จาก local — **⚠️ ยังไม่ยืนยันว่า login สำเร็จ**, งานนอก repo ไม่กระทบ src/
- 2026-08-28 — [ปรับแต่งประสิทธิภาพ 5 จุดตาม Code Review & ผล Load Test บน 6 Instances](handoff_28_08_2026_performance-tuning-and-review-fixes.md) — ปรับปรุง 5 ข้อ (Log rotation 10m/3 · ตัด duplicate pino logging · ใส่ mem_limit/cpus/NODE_OPTIONS · replica shared_buffers 256MB · distributed debounce cache invalidation) — **Throughput +37% (2,548 rps), checks 99.96%, Hit Ratio 97.63%, Data Integrity 100% (50/50)** · 35 tests ผ่าน
- 2026-08-27 — [ยิง k6 จริงครั้งแรก: เจอสต็อกรั่ว 8 ชิ้น](handoff_27_08_2026_load-test-first-run.md) — รัน container + ยิง k6 ครั้งแรกของโปรเจกต์ เจอข้อบกพร่อง 3 ชั้นที่บังหน้ากัน (`Dockerfile` EACCES · nginx retry amplification · `commandTimeout` ทำสต็อกรั่ว) — **แก้ครบทั้ง 3 · §9.3 ผ่านครบ 4 ข้อครั้งแรก (50/50)** · 35 tests ผ่าน
- 2026-08-26 — [Primer Template + เก็บกวาดลิงก์ old_architecture](handoff_26_08_2026_primer-template.md) — กู้ git ที่ตามหลัง 23 commits + สร้างแม่แบบ prompt เขียนเอกสารปูพื้นฐาน (`docs/Meta/`) — **scrutinize ด้วย 3 agent แล้วเขียนใหม่ทั้งฉบับ** · งานเอกสารล้วน ไม่แตะ src/
- 2026-08-26 — [Design Review รอบ 2 + แก้ 10 ข้อ](handoff_26_08_2026_design-review-round2.md) — เอกสารปูพื้นโค้ด (`docs/Codebase/`) + review ด้วย 4 agent แล้ว cross-examine — **เจอว่า blocker (b) ที่บันทึกว่าปิดแล้วยังเปิดอยู่** แก้ไป 10/11 ข้อ · build/lint/test ผ่าน (32 tests)
- 2026-08-26 — [Backend Implementation](handoff_26_08_2026_backend-implementation.md) — สร้าง `flash-sale-backend` ทั้งโปรเจกต์ (NestJS + docker-compose + loadtest.js) ด้วย 3 agent + 1 agent ตรวจ requirement — **merge เข้า main แล้ว (11 commits) แต่ยังไม่เคยรัน container / ยิง k6**
- 2026-08-26 — [Architecture Rationale + DB Schema](handoff_26_08_2026_architecture-rationale-db-schema.md) — เขียนสเปกสถาปัตยกรรมใหม่ทั้งชุด + เพิ่ม §3.1 DB schema + design review ด้วย 4 agent เจอ blocker 2 ข้อ — **จบแล้ว (ตัดสินใจครบ 4 ข้อในรอบถัดมา, commit แล้ว)**
